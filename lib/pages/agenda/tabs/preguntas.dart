import 'package:flutter/material.dart';

class Preguntas extends StatelessWidget {
  const Preguntas({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Pestaña de preguntas'),);
  }
}