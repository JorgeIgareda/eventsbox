import 'package:flutter/material.dart';

class Votaciones extends StatelessWidget {
  const Votaciones({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Pestaña de votaciones'),);
  }
}